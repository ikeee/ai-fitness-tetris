# 复刻核对报告 — AI 体感俄罗斯方块

核对对象：`D:\Downloads\体感控制计算机视觉这_20260913223204.mp4`（12 秒，1080×1920 竖屏，30fps，逐秒抽帧 12 张）
核对日期：2026-09-21

**总体结论：核心玩法与主体视觉已复刻，整体相似度约 80%，尚未达到 99%。** 主要差距集中在：画幅比例、底部控制区样式、棋盘边框标记、方块渲染样式、HUD 布局。

---

## ✅ 已复刻一致的部分

| 项目 | 原视频 | 复刻版 | 状态 |
|---|---|---|---|
| 摄像头实时全屏背景 | 客厅实拍画面铺满 | video 铺满 stage | ✅ |
| 中央 10×20 半透明网格棋盘 | 半透明黑底 + 白色网格线 | 同 | ✅ |
| 骨骼叠加（白线+关节点，肩/肘/腕/胯） | 白色骨架线 | 同 | ✅ |
| 头部移动 → 左右移动方块 | 是 | 是 | ✅ |
| 左手下压 → 左旋 / 右手下压 → 右旋 | 是 | 是 | ✅ |
| 双手下压 → 快速落块（DROP） | 是 | 是 | ✅ |
| 左上角 SCORE 黑色小面板 | `SCORE: 0` | 左上 HUD | ✅（文案略不同） |
| 右上角 NEXT 黑色预览面板 | 黑底 + 下一方块 | 同 | ✅ |
| 手势高亮反馈 | 底部按钮亮起绿色 | 有（文字形式） | ⚠️ 形式不同 |
| 消行、计分、7 种方块、彩色方块 | 有 | 有 | ✅ |

## ❌ 没跟上的部分（按影响排序）

### 1. 画幅与棋盘占比（差异最明显）
- 原视频：**9:16 竖屏全屏**，摄像头画面铺满整个手机屏；棋盘居中、宽度较窄，上下留大量实拍画面。
- 复刻版：桌面端默认 **16:9 横屏** stage（仅 <820px 宽才切竖屏），棋盘高度占 stage 的 86%，比视频里"悬浮在人物胸口前的小棋盘"占比大。
- 视频里棋盘区域约占屏高 40–45%，四周视频画面通透。

### 2. 底部控制区（差异明显）
- 原视频：底部居中只有 **3 个方括号按钮 `[ROT_L] [DROP] [ROT_R]`**，被触发的按钮会**整格变绿高亮**（如 f03 的 ROT_R、f11 的 DROP）。
- 复刻版：底部是 4 格文字条 `HEAD MOVE / L-DOWN ROT_L / R-DOWN ROT_R / BOTH DOWN DROP`，**无点击态高亮**，且把"头部移动"也放进了控制条（视频里没有）。

### 3. 左下角 / 右下角 HUD 内容不符
- 原视频左下角：黑色盒子里的**键盘/操作说明列表**（HEAD MOVE、L-DOWN ROT_L、R-DOWN ROT_R、BOTH DOWN DROP，等宽字体逐行列出）。
- 复刻版左下角：是"AI GESTURE"手势状态面板（视频里没有这个面板）。
- 原视频右下角：黑色键盘提示小盒；复刻版是摄像头状态面板（视频里没有）。

### 4. 棋盘边缘黄色 `‹›` 标记
- 原视频：黄色箭头标记出现在棋盘**左右两侧竖边（逐行）+ 底边（逐列）**，共三边。
- 复刻版：只有左边竖排 + 底边，且用 CSS `::before/::after` 绝对定位实现，位置是硬编码的（left:-245px 这类），不同尺寸下会跑偏。

### 5. 方块渲染样式
- 原视频：方块**填满格子**，格与格之间由网格线自然分隔，颜色为高饱和纯色，几乎无内边距/高光。
- 复刻版：方块有 2px 内缩、白色描边和顶部高光条，看起来比视频"圆润/卡通"一些。

### 6. 双重网格问题（复刻版自身 bug）
- 复刻版 `drawGridAndPose()` 在 overlay 上又画了一个独立网格（位置按 `h*.23` 估算），与棋盘 canvas 的网格**不重合**，视频中只有一套网格。应删除 overlay 里的网格，只保留棋盘 canvas 的。

### 7. 顶部标题区
- 原视频为纯全屏游戏画面（视频里的黄色标题是短视频平台字幕，不是游戏 UI）。
- 复刻版页面上方有网页标题区、底部有按钮控制栏——作为网页合理，但与"游戏画面本身"不一致。若要 99%，应让 stage 全屏、标题仅在 stage 外可选。

### 8. 其他小差异
- 视频 SCORE 文案为 `SCORE: 0` 单行；复刻版为 SCORE 标签 + 大数字两行。
- 视频中方块颜色观感：红/绿/青为主；复刻版配色一致 ✅，但黄色 O、紫色 T 在视频 12 秒内未出现，无法核对。
- 视频无暂停/重开/镜像开关 UI；复刻版有（功能增强，可保留）。

## 建议修复优先级

1. **P0** 删除 overlay 重复网格（bug）
2. **P0** 底部改为 `[ROT_L] [DROP] [ROT_R]` 三按钮 + 触发时绿色高亮
3. **P1** stage 默认竖屏比例（或全屏模式），棋盘占屏高 ~42%
4. **P1** 左下 HUD 改为操作说明列表，右下改键盘提示（摄像头状态挪进控制栏）
5. **P2** 三边黄色 `‹›` 标记，改为 JS 按行列数动态绘制
6. **P2** 方块去内边距/描边/高光，填满格子贴近视频观感
7. **P3** SCORE 改单行 `SCORE: 0`
